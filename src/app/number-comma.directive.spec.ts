import { NumberCommaDirective } from './number-comma.directive';

describe('NumberCommaDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberCommaDirective();
    expect(directive).toBeTruthy();
  });
});
